name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: install
        run: pip install -e ".[dev]"
      - name: lint
        run: |
          ruff check src tests scripts
          ruff format --check src tests scripts
      - name: typecheck
        run: mypy src
      - name: tests
        env:
          OPENAI_API_KEY: test
          MCP_API_KEYS: test-key-1,test-key-2
        run: pytest -v
